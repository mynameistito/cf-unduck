pre-commit:
  jobs:
    - run: bun ultracite fix
      glob: "**/*.{js,jsx,ts,tsx,json,jsonc,css}"
      stage_fixed: true

    - run: bun tsgo --noEmit
      glob: "**/*.{ts,tsx}"

    - run: bun run scripts/cleanup.ts
